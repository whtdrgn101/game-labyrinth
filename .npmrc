# Public npm registry — this repo is the Track D / D2c pilot and MUST resolve @game-hub/* from the
# registry, never from a workspace link back into the platform monorepo. Stated explicitly so a stray
# local .npmrc or a link: override can't quietly reintroduce the coupling the pilot exists to disprove.
registry=https://registry.npmjs.org/
